import { component$ } from '@builder.io/qwik';

export default component$(() => {

  return (
    <div class="fixed inset-0 flex justify-center items-center bg-[rgba(0,0,0,0.5)]">
        <img src="/tail-spin.svg" />
    </div>
  );
});
